<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:DataContext />
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top" Height="20" VerticalAlignment="Top">
            <MenuItem Header="_RaceStats" Click="Race_Statistics_OnClick">
                
            </MenuItem>
            <MenuItem Header="_CompetitionStats" Click="Competition_Statistics_OnClick">
                
            </MenuItem>
            <MenuItem Header="_Close" Click="Close_OnClick">
                
            </MenuItem>
        </Menu>
        
        <Grid DockPanel.Dock="Bottom">
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FEFEFE" Offset="0"/>
                    <GradientStop Color="#FEFEFE" Offset="1"/>
                </LinearGradientBrush>
            </Grid.Background>
            <Image Name="Track" VerticalAlignment="Bottom" HorizontalAlignment="Right" />

        </Grid>
    </DockPanel>
</Window>
